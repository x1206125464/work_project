环境：vs2015 + python3.6 + opencv3.2



project1基础：1、识别颜色目标物轮廓，标定其轮廓以及中心点，输出中心点坐标、BGR值
              2、python语言编程实现Kmeans算法并运用。对颜色目标物进行聚类


实现结果：1、摄像头
          2、启动时自动检测物体边缘轮廓
	  3、按o键对物体进行聚类（需要修改聚类簇的K值）（默认为4类）（内部auto_k_means函数有问题，使用k_means函数）
	  4、按s显示轮廓数量和对应坐标
	  5、按n显示轮廓对应的标号
	  6、按m显示画面Vchannel与Schannel